<h2 class="text-muted">
  <% if @posts.any? %>
    <%= pluralize(@posts.count, "Resultado") %> Encontrados :
  <% else %>
    Nenhum Resultado Encontrado
  <% end %>
</h2>

<% @posts.each do |post| %>
  <%= render post %>
<% end %>
